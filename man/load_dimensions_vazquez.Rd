% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_dimensions_amfiteatres_vazquez.R
\name{load_dimensions_vazquez}
\alias{load_dimensions_vazquez}
\title{Carrega i combina múltiples dataframes de dades d'amfiteatres}
\usage{
load_dimensions_vazquez(
  seleccionar_columnes = NULL,
  filtrar_edifici = NULL,
  filtrar_provincia = NULL,
  filtrar_pais = NULL,
  format_llarg = FALSE,
  etiquetes = FALSE
)
}
\arguments{
\item{seleccionar_columnes}{Selecciona les columnes desitjades per la sortida del dataframe.}

\item{filtrar_edifici}{Lògic. Accepta les següents opcions: amphitheater, arena_hippodrome, arena_stadium, gallo_roman, oval_structure i practice_arena.}

\item{filtrar_provincia}{Lògic. Accepta noms de provincies altimperials.}

\item{filtrar_pais}{Lògic. Accepta noms de païssos moderns en anglès.}

\item{format_llarg}{Lògic. Si és TRUE, el dataframe de sortida es retorna en format llarg (pivotat).}

\item{etiquetes.}{Lògic. Si és TRUE, el dataframe de sortida conté columnes etiqueta.}
}
\value{
Un dataframe de R amb les dades fusionades i estructurades dels amfiteatres romans i republicans.
}
\description{
Aquesta funció cerca arxius de dades d'amfiteatres amb un patró específic
en una carpeta donada, els llegeix com a dataframes amb un separador de punt i coma,
i els combina en un sol \code{tibble} utilitzant \code{bind_rows}.
}
\details{
La funció realitza els següents passos:
\enumerate{
\item Cerca arxius CSV amb un patró específic a una carpeta donada.
\item Llegeix cada arxiu i els emmagatzema en una llista de dataframes.
\item Renombra i homogeneïtza les columnes de cada dataframe.
\item Calcula noves columnes derivades com la ràtio de l'arena, superfícies i perímetres.
\item Aplica filtres opcionals per tipus d'edifici o selecció de columnes.
\item Combina tots els dataframes de la llista en un únic 'tibble' ordenat.
}
}
\examples{
\dontrun{
# Carregar totes les dades de Vazquez
df_vazquez_complet <- load_dimensions_vazquez()

# Carregar dades de Vazquez filtrant per tipus d'edifici
df_vazquez_filtrat <- load_dimensions_vazquez(
 filtrar_edifici = 'amphitheater')

# Carregar dades de Vazquez seleccionant columnes
df_vazquez_seleccionat <- load_dimensions_vazquez(
 filtrar_edifici = 'amphitheater',
 filtrar_provincia = 'hispania',
 seleccionar_columnes = c(contains('amplada'), contains('alcada'), -contains('cavea'), 'bib'))

# Carregar dades de Vazquez en format llarg
df_vazquez_llarg <- load_dimensions_vazquez(format_llarg = TRUE)
}

}
\seealso{
\code{\link[dplyr]{mutate}}, \code{\link[dplyr]{arrange}}

\code{\link[tibble]{tibble}}

\code{\link[rlang]{enquo}}
}
