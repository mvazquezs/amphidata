% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_setup.R
\name{format_dtable}
\alias{format_dtable}
\title{Aplica formats comuns a taules DT}
\usage{
format_dtable(dt_object, columns, format, ...)
}
\arguments{
\item{dt_object}{L'objecte \code{datatable} al qual s'aplicarà el format. Ha de ser
el resultat d'una crida a \code{DT::datatable()} o d'una crida anterior a \code{format_dtable}.}

\item{columns}{Un vector de noms de columna a les quals aplicar el format.}

\item{format}{Un string que especifica el tipus de format a aplicar. Valors
vàlids són: \code{"style"}, \code{"round"}, \code{"percentage"}.}

\item{...}{Arguments addicionals que es passen a la funció de format de \code{DT}
corresponent.
\itemize{
\item Per a \code{format = "style"}: \code{n_intervals} (per defecte \code{19}), \code{paleta} (una funció \code{colorRampPalette}).
\item Per a \code{format = "round"}: \code{digits} (per defecte \code{2}).
\item Per a \code{format = "percentage"}: \code{digits} (per defecte \code{0}).
}}
}
\value{
L'objecte datatable amb el format aplicat.
}
\description{
Aquesta funció és un embolcall (\code{wrapper}) que simplifica l'aplicació
de formats comuns a les taules creades amb el paquet \code{DT}. Permet encadenar
formats com \code{formatStyle}, \code{formatRound} i \code{formatPercentage} d'una manera
declarativa i consistent.
}
\examples{
if (interactive()) {
  library(DT)

  data <- data.frame(
    Producte = LETTERS[1:5],
    Vendes = c(1500, 800, 2200, 500, 1200),
    Creixement = c(0.053, -0.021, 0.128, 0.015, 0.08),
    QuotaMercat = c(0.25, 0.12, 0.35, 0.08, 0.20)
  )

  DT::datatable(data) \%>\%
    format_dtable(columns = "Vendes", format = "style") \%>\%
    format_dtable(columns = "Creixement", format = "percentage", digits = 1) \%>\%
    format_dtable(columns = "QuotaMercat", format = "round", digits = 3)
}
}
\seealso{
Documentació oficial de DT: \url{https://rstudio.github.io/DT/}
Referència de funcions de format: \url{https://rstudio.github.io/DT/functions.html}
}
