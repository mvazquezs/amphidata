% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_dimensions_amfiteatres_golvin.R
\name{load_dimensions_golvin}
\alias{load_dimensions_golvin}
\title{Extracció, estructuració i fusió de dades d'amfiteatres romans}
\usage{
load_dimensions_golvin(
  filtrar_provincia = NULL,
  filtrar_pais = NULL,
  seleccionar_columnes = NULL,
  format_llarg = FALSE,
  etiquetes = FALSE
)
}
\arguments{
\item{filtrar_provincia}{Lògic. Accepta noms de provincies altimperials.}

\item{filtrar_pais}{Lògic. Accepta noms de païssos moderns en anglès.}

\item{seleccionar_columnes}{Selecciona les columnes desitjades per la sortida del dataframe.}

\item{format_llarg}{Lògic. Si és TRUE, el dataframe de sortida es retorna en format llarg (pivotat).}

\item{etiquetes.}{Lògic. Si és TRUE, el dataframe de sortida conté columnes etiqueta.}
}
\value{
Un \code{tibble} amb les dades fusionades i estructurades.
}
\description{
Aquesta funció processa dades d'amfiteatres romans i republicans extretes de taules,
calcula mètriques derivades i fusiona les dades en un únic dataframe.
}
\details{
La funció realitza els següents passos:
\enumerate{
\item Extreu les dades de les taules originals de Golvin (1988).
\item Calcula mètriques derivades com ràtios, superfícies i perímetres.
\item Fusiona totes les taules en un únic \code{tibble}.
\item Permet filtrar, seleccionar i transformar les dades.
}
}
\examples{
\dontrun{
# Carregar totes les dades de Golvin
df_golvin_complet <- load_dimensions_golvin()

# Carregar dades de Golvin filtrant per província
df_golvin_hispania <- load_dimensions_golvin(
  filtrar_provincia = 'hispania'
)

# Carregar dades de Golvin seleccionant columnes
df_golvin_seleccionat <- load_dimensions_golvin(
  filtrar_provincia = 'hispania',
  seleccionar_columnes = c(nom, pais, amplada_general, alcada_general)
)

# Carregar dades de Golvin en format llarg
df_golvin_llarg <- load_dimensions_golvin(
  seleccionar_columnes = c(amplada_arena, alcada_arena),
  format_llarg = TRUE
)
}

}
\seealso{
\code{\link[dplyr]{mutate}}

\code{\link[tidyr]{pivot_longer}}
}
