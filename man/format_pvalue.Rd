% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_funcions_accessories.R
\name{format_pvalue}
\alias{format_pvalue}
\title{Adjust p-values for multiple comparisons}
\usage{
format_pvalue(
  df,
  p_col = "p",
  p_digits = 3,
  method = c("holm", "hochberg", "hommel", "bonferroni", "BH", "BY", "fdr", "none"),
  trend = FALSE
)
}
\arguments{
\item{df}{The 'data.frame' containing the p-value column.}

\item{p_col}{The name of the column containing the raw (unadjusted) p-values.
Defaults to \code{"p"}, which is the common column name generated by \code{rstatix}.}

\item{p_digits}{Defaults to 3. Rounds the final p-value to this number of decimals.}

\item{method}{Method for adjusting p-values. Includes 'holm', 'hochberg', 'hommel',
'bonferroni', 'BH', 'BY', 'fdr', 'none'. Defaults to 'holm'.}

\item{trend}{Logical. If \code{TRUE}, uses a "trend" approach to
textually represent adjusted p-values (e.g., 'P < 0.1'). Defaults to \code{FALSE}.}
}
\value{
A 'data.frame' with the new columns added: \code{raw_pval_text},
\code{raw_pval_signif}, \code{adj_pval}, \code{adj_pval_text}, and \code{adj_pval_signif}.
}
\description{
This 'pipe-friendly' function adds columns with adjusted p-values
and their textual representation to a 'data.frame'.
}
\examples{
\dontrun{
library(rstatix)
# Create an example data.frame
df_test <- data.frame(
  group = c("A", "B", "C"),
  p = c(0.001, 0.02, 0.1)
)

# Adjust p-values using the default 'p' column
format_pvalue(df_test, method = "bonferroni")

# Adjust p-values specifying a different column name
df_test2 <- data.frame(
  group = c("A", "B", "C"),
  p.value = c(0.001, 0.02, 0.1)
)
format_pvalue(df_test2, p_col = "p.value", method = "holm")
}
}
\seealso{
\code{\link[rstatix]{adjust_pvalue}}
}
